# Copy to .env on the VPS and fill in. NEVER commit the real .env.
#
# Generate the password on the server, not in a chat window or a notes app:
#   openssl rand -base64 36 | tr -d '/+=' | cut -c1-40

POSTGRES_USER=only2bali
POSTGRES_PASSWORD=
POSTGRES_DB=only2bali

# Nightly dumps kept for this many days.
BACKUP_RETENTION_DAYS=14
